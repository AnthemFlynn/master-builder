{
  "name": "kingdom-builder",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "bun build.ts",
    "dev": "bun build.ts && bun serve.ts",
    "preview": "bun build.ts && bun serve.ts",
    "serve": "bun serve.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "simplex-noise": "^4.0.3",
    "three": "^0.181.2"
  },
  "devDependencies": {
    "@types/three": "^0.181.0",
    "typescript": "^5.0.0",
    "vite": "^7.2.7"
  }
}